{{#if showList}}
  <table class="table">
    <thead>
      <tr>
        <br>
        <h2 class="text-danger mt-2">Productos cargados</h2>
        <br>
        <th scope="col">#</th>
        <th scope="col">Título</th>
        <th scope="col">Precio</th>
        <th scope="col">Imagen</th>
      </tr>
    </thead>
    <tbody>
      {{#each list}}
        <tr>
          <th scope="row">{{this.id}}</th>
          <td>{{this.title}}</td>
          <td>{{this.price}}</td>
          <td>
            <img width="30px" src={{this.thumbnail}} alt="Img-Productos">
          </td>
        </tr>
      {{/each}}
    </tbody>
  </table>
{{else}}
  <h5 class="alert-danger rounded p-2">No se encontraron datos</h5>
{{/if}}